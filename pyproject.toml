[project]
name = "barkup"
version = "0.1.0"
description = "Barkup is a Python-based CLI tool for automating personal backups to local and cloud storage, supporting providers such as Google Drive and Amazon S3."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
